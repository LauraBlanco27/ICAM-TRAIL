import 'package:flutter/material.dart';

class SistemaSilvoagricola extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Sistema Silvoagricola')),
      body: Center(child: Text('Página de SistemaSilvoagricola')),
    );
  }
}