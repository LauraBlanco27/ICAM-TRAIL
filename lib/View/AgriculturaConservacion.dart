import 'package:flutter/material.dart';

class AgriculturaConservacion extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Agricultura de Conservacion')),
      body: Center(child: Text('Página de Agricultura de Conservacion')),
    );
  }
}