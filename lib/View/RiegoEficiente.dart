import 'package:flutter/material.dart';

class RiegoEficiente extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Riego Eficiente')),
      body: Center(child: Text('Página de Riego Eficiente')),
    );
  }
}