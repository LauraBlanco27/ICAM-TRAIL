import 'package:flutter/material.dart';

class ZanjaBordo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('ZanjaBordo')),
      body: Center(child: Text('Página de ZanjaBordo')),
    );
  }
}