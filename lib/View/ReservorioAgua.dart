import 'package:flutter/material.dart';

class ReservorioAgua extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Reservorio de Agua')),
      body: Center(child: Text('Página de Reservorio de Agua')),
    );
  }
}