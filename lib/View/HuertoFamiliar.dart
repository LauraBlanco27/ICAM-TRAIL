import 'package:flutter/material.dart';

class HuertoFamiliar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Huerto Familiar')),
      body: Center(child: Text('Página de Huerto Familiar')),
    );
  }
}